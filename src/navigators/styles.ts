import {StyleSheet} from 'react-native';

export const navStyles = StyleSheet.create({
  headerTitle: {
    fontFamily: 'Gilroy-Bold',
    fontSize: 24,
  },
  defaultHeader: {
    borderBottomWidth: 0,
    shadowOpacity: 0,
    elevation: 0,
  },
});
